<!DOCTYPE html>
<html>
  <head>
    <title>BlogApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
  <% if admin_user_signed_in? %>
   <button><%= link_to 'Log out', destroy_admin_user_session_path, data:{turbo-method: :delete } %></button>
   <% else %>
    <button><%= link_to 'Log in', new_admin_user_session_path %></button>
  <% end %>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <%= yield %>
  </body>
</html>
