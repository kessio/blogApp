<!DOCTYPE html>
<html>
  <head>
    <title>BlogApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    <h1>BlogApp</h1>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <% if admin_user_signed_in? %>
    <%= button_to 'Sign out', destroy_admin_user_session_path(@user), method: :delete, data: { turbo: false } %>
    <% else %>
    <%= button_to 'Sign in', new_admin_user_session_path, data: { turbo: false } %>
    <% end %>
    <%= yield %>
  </body>
</html>
